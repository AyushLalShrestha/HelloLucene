# Build lucene indexes from csv file source & search them.
 1. Build lucene indexes by spawning threads to build them from various sources
 2. URI exposed to search the products by providing keywords

